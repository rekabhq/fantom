import 'package:fantom/src/generator/utils/generation_data.dart';
import 'package:fantom/src/openapi/model/model.dart';

class Generator {
  Generator();

  static GenerationData generate(OpenApi openApi) {
    //TODO: should generate models and apis using [ApiClassGenerator] & [ComponentsGenerator] and return a GenerationData
    throw UnimplementedError(
      'Generator.generate(openApi) is not yet implemented',
    );
  }
}
